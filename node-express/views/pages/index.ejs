<html>
<head></head>
<body>
<form>
    <input type="date" value="<%= startDate %>" name="start"/>
    <input type="date" value="<%= endDate %>" name="end"/>
    <button type="submit">Search</button>
</form>

<ul>
    <% events.forEach(function(event) { %>
        <li><%= event.Date %> - <%= event.Name %> - <%= event.Location_name %></li>
    <% }); %>
</ul>
</body>
</html>